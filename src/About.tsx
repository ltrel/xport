import { Container, Typography } from "@mui/material";

export default function About() {
  return (
    <Container maxWidth='lg'>
      <Typography>
        xport is a stock portfolio tracking tool
      </Typography>
    </Container>
  );
}
